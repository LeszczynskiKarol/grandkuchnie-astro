---
// ProcessSection.astro - Jak działamy?
const steps = [
  {
    title: 'Darmowa wycena',
    description: 'Zadzwoń lub napisz do nas już teraz i daj znać, jaką przestrzeń mamy dla Ciebie stworzyć, a zaproponujemy indywidualną, bezpłatną wycenę.',
  },
  {
    title: 'Projekt',
    description: 'Nasz zespół tworzą kompotentni projektanci, któzy z przyjemnością przygotują dla Ciebie koncepcję. Zapraszamy do współpracy również, jeśli masz już gotowy projekt.',
  },
  {
    title: 'Produkcja',
    description: 'Meble i inne elementy wnętrza tworzymy od podstaw w naszym zakładzie. Wykorzystujemy praktyczne, sprawdzone rozwiązania i starannie dobrane materiały, aby zapewnić najlepszy efekt.',
  },
  {
    title: 'Montaż',
    description: 'Ostatnim etapem współpracy jest montaż, który nasi specjaliści wykonują w Twoim domu. Dbamy o każdy najmniejszy szczegół realizacji, aby oddać do dyspozycji Twoją wymarzoną przestrzeń.',
  },
];
---

<section class="py-24 bg-white">
  <div class="max-w-7xl mx-auto px-4">
    <h2 class="text-4xl font-bold text-center text-secondary mb-16">
      Jak działamy?
    </h2>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
      {steps.map((step, index) => (
        <div class="relative">
          {/* Arrow connecting (except last item) */}
          {index < steps.length - 1 && (
            <div class="hidden lg:block absolute -right-3 top-1/2 transform -translate-y-1/2 translate-x-full z-10">
              <svg class="w-6 h-6 text-primary" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
              </svg>
            </div>
          )}

          <div class="bg-gradient-to-br from-secondary to-secondary/80 text-white p-8 rounded-2xl h-full">
            <div class="flex flex-col h-full">
              <div class="mb-6">
                <span class="bg-primary text-secondary font-bold text-2xl w-12 h-12 rounded-full flex items-center justify-center mb-4">
                  {index + 1}
                </span>
                <h3 class="text-xl font-bold mb-2">{step.title}</h3>
                <p class="text-gray-200">{step.description}</p>
              </div>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>